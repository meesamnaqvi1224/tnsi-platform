# @tnsi/core

Framework-free domain layer: entities, domain-event definitions, and pure business logic (e.g. assessment scoring). Depends on nothing infrastructure-specific; everything else depends on it.
