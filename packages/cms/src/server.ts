// Server-only exports
// This module can only be imported in Server Components, API routes, and other server contexts.
// Importing it in client components will cause a build error.

export { sanityFetch } from './lib/fetch';
