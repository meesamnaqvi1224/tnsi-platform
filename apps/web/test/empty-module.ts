// Stub for the `server-only` package under vitest - see vitest.config.ts's
// `alias` for why this exists. Intentionally empty: `server-only` itself
// has no real exports either, it's a Next.js build-time marker.
export {};
