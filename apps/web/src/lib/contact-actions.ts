'use server';

/**
 * Placeholder server action for contact form submissions.
 * Replace with email service or CRM integration.
 */
export async function submitContactForm(formData: FormData): Promise<void> {
  void formData;
  // Future: validate, send email, log to CRM
}
