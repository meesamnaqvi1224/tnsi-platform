# studio (reserved)

Reserved workspace member for the standalone Sanity Studio app, deployed independently of `apps/web` so the content team's release cadence is decoupled from engineering's (see ARCHITECTURE.md).

Not yet built: the `sanity` package is explicitly out of scope for the current sprint and requires CMS-vendor approval before this app is scaffolded.
