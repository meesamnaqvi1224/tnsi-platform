# admin (reserved)

Reserved workspace member for the internal back office: entitlement overrides, CRM sync monitoring, support tooling (see ARCHITECTURE.md). Not customer-facing.

Not yet built: no framework has been chosen for this app yet. Likely Next.js to share `packages/ui` and `packages/auth`, but that decision is deferred to the sprint that scaffolds it.
