# docs (reserved)

Reserved workspace member for the internal engineering documentation site (ADRs, onboarding, package reference) — distinct from the root-level `docs/` folder, which holds product/business markdown (see ARCHITECTURE.md).

Not yet built: a docs-site tool (e.g. Nextra) needs to be chosen before this app is scaffolded.
